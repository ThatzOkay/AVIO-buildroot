name: Build images

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: rpi4
            board: raspberrypi4
          - service: rpi5
            board: raspberrypi5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Check producer/consumer defconfig sync
        run: bash avio_configs/scripts/check-toolchain-sync.sh

      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android /opt/hostedtoolcache

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: .ccache
          key: ccache-v2-${{ matrix.board }}-${{ github.run_id }}
          restore-keys: |
            ccache-v2-${{ matrix.board }}-

      - name: Download prebuilt toolchain
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p "toolchains/${{ matrix.board }}"
          if ! gh release download toolchains \
              --repo "${{ github.repository }}" \
              --pattern "toolchain-${{ matrix.board }}.tar.gz" \
              --output "toolchain-${{ matrix.board }}.tar.gz" \
              --clobber; then
            echo "::error::No prebuilt toolchain asset for ${{ matrix.board }} on the 'toolchains' release. Run the 'Build toolchain' workflow (workflow_dispatch) first."
            exit 1
          fi
          tar xzf "toolchain-${{ matrix.board }}.tar.gz" -C "toolchains/${{ matrix.board }}"
          chmod -R 777 toolchains
          "toolchains/${{ matrix.board }}/relocate-sdk.sh" "/app/toolchains/${{ matrix.board }}"

      - name: Download prebuilt webkitgtk (optional speedup)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release download prebuilt-artifacts \
              --repo "${{ github.repository }}" \
              --pattern "webkitgtk-${{ matrix.board }}.tar.gz.part-*" \
              --clobber; then
            cat webkitgtk-${{ matrix.board }}.tar.gz.part-* > "webkitgtk-${{ matrix.board }}.tar.gz"
            rm -f webkitgtk-${{ matrix.board }}.tar.gz.part-*
            echo "Prebuilt webkitgtk found; the build step will restore it into output/${{ matrix.board }} before running make."
          else
            echo "::warning::No prebuilt webkitgtk asset for ${{ matrix.board }} yet. Run the 'Build webkitgtk' workflow (workflow_dispatch) to speed up future builds. Continuing without it."
          fi

      - name: Prepare bind-mounted directories
        run: |
          mkdir -p .ccache images
          chmod -R 777 .ccache images

      - name: Build ${{ matrix.board }} image
        run: docker compose run --rm ${{ matrix.service }}

      - name: Upload ${{ matrix.board }} artifact
        uses: actions/upload-artifact@v4
        with:
          name: sdcard-${{ matrix.board }}
          path: images/sdcard-${{ matrix.board }}.img
          if-no-files-found: error
